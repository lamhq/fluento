{
  "name": "infra",
  "description": "Infrastructure code for the project",
  "scripts": {
    "init": "terraform init -backend-config=config/s3.tfbackend",
    "plan:dev": "terraform workspace select -or-create dev && terraform plan -var-file=config/dev.tfvars",
    "deploy:dev": "terraform workspace select -or-create dev && terraform apply -var-file=config/dev.tfvars -auto-approve",
    "output": "terraform output"
  }
}
